# Copier en `.env` (gitignoré) pour activer l'envoi des logs vers la stack ELK
# (elk/docker-compose.yml). Sans .env, le transport Logstash reste désactivé.

# Nom du service Logstash sur le réseau `orion` — laisser vide pour désactiver
LOGSTASH_HOST=logstash
LOGSTASH_PORT=5000
