{
  "name": "orion-release-tooling",
  "private": true,
  "description": "Outillage de release (semantic-release), verrouillé par lockfile — cf. DOCUMENTATION.md § 8.2. Le code applicatif vit dans server/ et client/.",
  "scripts": {
    "release": "semantic-release"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "devDependencies": {
    "@semantic-release/exec": "7.1.0",
    "semantic-release": "25.0.8"
  }
}
