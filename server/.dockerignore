# Dépendances et sorties de build (reconstruites dans l'image)
node_modules
dist
coverage

# Secrets et données locales : ne doivent jamais entrer dans l'image
.env*
*.db
*.db-journal

# Divers
*.log
*.tsbuildinfo
Dockerfile
.dockerignore
